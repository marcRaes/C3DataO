{% extends 'base.html.twig' %}

{% block title %}Liste des véhicules{% endblock %}

{% block body %}
    {% import '_category.html.twig' as categoryMacros %}
    {% set keysToLabel = ['cost_in_credits', 'length', 'max_atmosphering_speed', 'crew', 'passengers', 'cargo_capacity'] %}

    {{ categoryMacros.renderCategory(listOfVehicles, keysToLabel, 'vehicles', 'app_vehicles', page, nbPages) }}
{% endblock %}