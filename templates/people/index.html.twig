{% extends 'base.html.twig' %}

{% block title %}Liste des personnages{% endblock %}

{% block body %}
    {% import '_category.html.twig' as categoryMacros %}
    {% set keysToLabel = ['height', 'mass', 'birth_year'] %}
    {% set unitKeys = {'height': 'cm', 'mass': 'kg'} %}

    {{ categoryMacros.renderCategory(listOfPeople, keysToLabel, 'people', 'app_people', page, nbPages, unitKeys=unitKeys) }}
{% endblock %}
