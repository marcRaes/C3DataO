{% extends 'base.html.twig' %}

{% block title %}Liste des planètes{% endblock %}

{% block body %}
    {% import '_category.html.twig' as categoryMacros %}
    {% set keysToLabel = ['rotation_period', 'orbital_period', 'diameter', 'surface_water', 'population'] %}

    {{ categoryMacros.renderCategory(listOfPlanets, keysToLabel, 'planets', 'app_planets', page, nbPages) }}
{% endblock %}