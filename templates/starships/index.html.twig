{% extends 'base.html.twig' %}

{% block title %}Liste des vaisseaux spatiaux{% endblock %}

{% block body %}
    {% import '_category.html.twig' as categoryMacros %}
    {% set keysToLabel = ['cost_in_credits', 'length', 'max_atmosphering_speed', 'crew', 'passengers', 'cargo_capacity', 'hyperdrive_rating', 'MGLT'] %}

    {{ categoryMacros.renderCategory(listOfStarships, keysToLabel, 'starships', 'app_starships', page, nbPages) }}
{% endblock %}