{% extends 'base.html.twig' %}

{% block title %}Liste des films{% endblock %}

{% block body %}
    {% import '_category.html.twig' as categoryMacros %}
    {% set keysToLabel = [] %}
    {% set excludeKeys = ['episode_id'] %}
    {% set specialKeys = {'opening_crawl': 'opening_crawl', 'release_date': 'release_date'} %}

    {{ categoryMacros.renderCategory(listOfFilms, keysToLabel, 'films', 'app_films', page, nbPages, excludeKeys, specialKeys) }}
{% endblock %}
